/**
 * @jsx React.DOM
 */

var React = require('react');

var ProgressComponent = React.createClass({
    render: function() {
        return <div className="progress_component">progress bar</div>
    }
});

module.exports = ProgressComponent;